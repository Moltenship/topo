{
  "name": "@molten-voice/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./dictation": "./src/dictation.ts",
    "./settings": "./src/settings.ts",
    "./ipc": "./src/ipc.ts"
  },
  "scripts": {
    "typecheck": "tsgo --project tsconfig.json --noEmit",
    "fmt": "oxfmt --check .",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests",
    "build": "tsgo --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "effect": "^3.21.2"
  }
}
