{
  "name": "@molten-voice/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --project tsconfig.json --noEmit",
    "fmt": "oxfmt --check .",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests",
    "build": "tsgo --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13"
  }
}
