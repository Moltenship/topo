{
  "name": "@molten-voice/asr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --project tsconfig.json --noEmit",
    "fmt": "oxfmt --check .",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests",
    "build": "tsgo --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@molten-voice/audio": "workspace:*",
    "@molten-voice/shared": "workspace:*",
    "ai": "^6.0.177",
    "effect": "^3.21.2"
  }
}
